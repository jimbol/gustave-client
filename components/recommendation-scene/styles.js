'use strict';

import React, {StyleSheet} from 'react-native';

export default StyleSheet.create({
  scene: {
  },

  commitButton: {
    position: 'absolute',
    bottom: 32,
    right: 16,
    backgroundColor: '#4a0b49',
  },
});
