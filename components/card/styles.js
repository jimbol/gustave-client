'use strict';

import React, {StyleSheet} from 'react-native';

export default StyleSheet.create({
  card: {
    margin: 16,
    flexDirection: 'column',
    shadowColor: "#000",
    shadowOpacity: .4,
    shadowRadius: 2,
    shadowOffset: {
      height: 2,
      width: 0
    },
    borderRadius: 0,
    borderWidth: 0,
    borderColor: '#ddd',
    backgroundColor: '#fff',
    flex: 1
  },

});
